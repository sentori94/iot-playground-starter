spring:
  datasource:
    url: jdbc:postgresql://iot-sensors-db.czsa8wkgcnmg.eu-west-3.rds.amazonaws.com:5432/postgres?sslmode=require
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: Eyeshield21
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
  thymeleaf:
    cache: false
    encoding: UTF-8
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate.jdbc.time_zone: UTC
      hibernate.format_sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration

management:
  endpoints:
    web:
      exposure:
        include: prometheus
  metrics:
    export:
      prometheus:
        enabled: true

logging:
  level:
    root: INFO
    com.sentori.iot: DEBUG

app:
  base-url: http://localhost:8080
  sensor:
    ingest-path: /sensors/data
  grafana:
    base-url: http://localhost:3000
    # UID + slug de TON dashboard (ceux de ton exemple)
    dashboard-path: /d/8ce481e7-1658-4a20-9d8e-ed54a564a5ae/sensors-temperature-par-capteur-user-run-new
    org-id: 1
    ds-prom-uid: dew0ddwep0p34d
    timezone: browser
    default-from: now-15m
    default-to: now
    default-refresh: 5s

server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true