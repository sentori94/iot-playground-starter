version: '3.8'

services:
  prometheus:
    image: prom/prometheus
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./docker/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
    networks:
      - monitoring

  grafana:
    image: grafana/grafana
    container_name: grafana
    ports:
      - "3000:3000"
    environment:
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana-storage:/var/lib/grafana
      - ./docker/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./docker/grafana/dashboards:/var/lib/grafana/dashboards:ro
    networks:
      - monitoring
    depends_on:
      - prometheus

  postgres:
    image: postgres:15
    container_name: iot-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: iot_playground_local
      POSTGRES_USER: iot_user
      POSTGRES_PASSWORD: iot_pass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U iot_user -d iot_playground_local"]
      interval: 10s
      timeout: 5s
      retries: 5

  xray-daemon:
    image: amazon/aws-xray-daemon
    container_name: xray-daemon
    ports:
      - "2000:2000/udp"
    environment:
      - AWS_ACCESS_KEY_ID=${XRAY_AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${XRAY_AWS_SECRET_ACCESS_KEY}
      - AWS_REGION=${AWS_REGION:-eu-west-3}
    command: -o
    networks:
      - monitoring

networks:
  monitoring:

volumes:
  grafana-storage:
  pgdata:
